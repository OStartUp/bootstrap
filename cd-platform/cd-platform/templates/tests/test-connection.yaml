apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cd-platform.fullname" . }}-test-connection"
  labels:
{{ include "cd-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "cd-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
