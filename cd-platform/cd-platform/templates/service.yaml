apiVersion: v1
kind: Service
metadata:
  name: {{ include "cd-platform.fullname" . }}
  labels:
    {{- include "cd-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cd-platform.selectorLabels" . | nindent 4 }}
